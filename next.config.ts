// next.config.ts

/*
// @ts-ignore
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // @ts-ignore
  experimental: {
    appDir: true,
  },
};
*/
const nextConfig = {
  experimental: {
    appDir: true,
  },
};

export default nextConfig;