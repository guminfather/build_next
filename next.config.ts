// next.config.ts

/** @type {import('next').NextConfig} */
const nextConfig = {
  // 불필요한 experimental 설정 제거
  // 필요한 다른 설정은 여기에 추가 가능
  reactStrictMode: false //중복실행제거
};

export default nextConfig;